1. Array is a special variable that contain more than on value
Simple Syntex of array :

var Simple = ["simple1","simple2","simple3"];
console.log(Simple);
console.log(simple[0]);
console.log(simple[1]);
console.log(simple[2]);

Why We need to use array ?
if we have a list of items and want to add in single variable then array helpful.

